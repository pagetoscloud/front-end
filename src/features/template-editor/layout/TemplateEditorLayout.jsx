import { Layout } from "./TemplateEditorLayout.styled";

export default function TemplateEditorLayout({children}){
    return (
        <Layout>
            {
                children
            }
        </Layout>
    )
}